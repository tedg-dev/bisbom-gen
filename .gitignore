# ============================================================
# Cloned target repositories (large, regenerable via git clone)
# ============================================================
repos/*/
!repos/.gitkeep

# ============================================================
# Raw output artifacts (regenerable via analysis scripts)
# ============================================================
output/omnibor/*/**
output/spdx/*/**
output/binaries/*/**
output/binary-scan/*/**
output/build-logs/*/**
output/runtime/**
!output/runtime/**/baseline.json
!output/**/.gitkeep

# ============================================================
# User-specific infrastructure profile
# ============================================================
# active-profile.md is tracked (private repo) so the setup
# is recoverable from a fresh clone. It contains no secrets —
# only instance IDs, IPs, SSH alias, and power-management commands.

# ============================================================
# Credentials and secrets
# ============================================================
keys.json
*.env
.env.*
!.env.example

# ============================================================
# Python
# ============================================================
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
.eggs/
*.egg
.pytest_cache/
.coverage
htmlcov/

# ============================================================
# Docker
# ============================================================
.docker/

# ============================================================
# IDE and editor files
# ============================================================
.vscode/
.idea/
.cursor/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ============================================================
# OS generated files
# ============================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ============================================================
# Terraform
# ============================================================
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
terraform/*.tfplan
terraform/tfplan

# ============================================================
# Logs and temp files
# ============================================================
*.log
*.tmp
*.bak

# ============================================================
# Draw.io backup files
# ============================================================
.$*.drawio.bkp

# ============================================================
# Ephemeral analysis output (build logs and runtime docs)
# ============================================================
docs/build-logs/
docs/runtime/

# ============================================================
# Session notes (local-only, not committed)
# ============================================================
session-notes.md
progress.txt

# ============================================================
# Archived golden baselines (local-only backup, not committed)
# Retired baselines kept around for a few cycles as a safety net.
# ============================================================
tests/golden/_archive/
