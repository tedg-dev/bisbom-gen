# Isolates: configuration-on-demand. With a single-project primary task,
# Gradle configures only the projects it needs; the capture must still
# force every project to register omniborDeps so no section is dropped.
org.gradle.configureondemand=true
