# Isolates: the configuration cache. The capture mutates
# startParameter.taskNames and registers tasks at configuration time; both
# must survive being serialized/reused by the configuration cache. Problems
# are set to warn so a violation surfaces as a report rather than aborting,
# letting the test assert the exact behaviour.
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn
